import React from "react";
import { Typography as MuiCoreTypography } from "@mui/material";

const Typography = (children) => {
  return (
    <MuiCoreTypography>
      {children}
    </MuiCoreTypography>
  )
}

export default Typography;
